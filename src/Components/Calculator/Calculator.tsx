import ToppingsDropdown from "../PizzaSizeDropdown/ToppingsDropdown";
import PizzaSizeDropdown from "../PizzaSizeDropdown/PizzaSizeDropdown";

import React from 'react'

const TotalPrice: number;// TotalPrice = name + toppings

export default function Calculator(TotalPrice:number,) {
  return (
    <div>Calculator</div>
  )
}
